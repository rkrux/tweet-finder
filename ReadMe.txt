To run the application, you need to add your valid Twitter application credentials in credentials.json. 
Go to folder directory and type 'php index.php' in terminal to run the application. It will fetch and display the tweets as per the given conditions.
