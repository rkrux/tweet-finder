To run the application, you need to add your valid Twitter application credentials in credentials.json. 
Go to folder directory and type 'php index.php' in terminal to run the application. 
Enter the search query. It will fetch and display the tweets.
Type QUIT to exit the application.
